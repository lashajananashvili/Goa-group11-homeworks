const titleElement = document.getElementById("title");
console.log(titleElement.textContent);

titleElement.textContent = "Welcome to JavaScript Programming";

const descriptionElement = document.getElementById("description");
descriptionElement.textContent = "We are learning about objects and the Document Object Model (DOM).";
